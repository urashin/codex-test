# Live Code Slide – Quickstart

```bash
# 1. Clone
$ git clone https://github.com/your-org/live-code-slide.git
$ cd live-code-slide

# 2. Local bootstrap
$ cp .env.example .env
$ docker compose up -d --build
$ open http://localhost:3000
```

